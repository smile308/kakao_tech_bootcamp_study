# 99. 다음 AI 작업 인수인계 문서

이 문서는 지금까지 진행한 프로젝트 학습 계획, 실제 코드 확인 범위, 사용자가 요구한 설명 형식, 발견된 오류와 다음 작업 순서를 한 곳에 모은 인수인계 문서입니다. 다음 AI는 이 문서와 실제 백엔드·프론트엔드 저장소를 함께 읽고 작업을 이어가야 합니다.

이 문서는 새 학습 장이 아닙니다. 다음 AI가 이전 대화를 볼 수 없어도 같은 기준을 유지하도록 만드는 작업 기준서입니다. 학습 장 번호와 분리하기 위해 파일 번호는 99번으로 둡니다.

작성 기준일: 2026-08-05

---

## 1. 기준 저장소와 문서 위치

### 백엔드

`/Users/miles/Documents/GitHub/KTB4_Miles_Week12_Back`

### 프론트엔드

`/Users/miles/Documents/GitHub/KTB4_Miles_Week12_Front`

### 학습 문서

`/Users/miles/Documents/GitHub/kakao_tech_bootcamp_study/study12/project-study/actual-code-learning`

현재 학습 문서:

- `00_실제코드_파일목록과_학습지도.md`
- `01_실행기반_build_profile_main.md`
- `02_회원가입_실제흐름.md`
- `03_이미지검증과_예외응답.md`
- `04_인증_로그인_자격증명과_세션생성.md`
- `05_인증_AccessToken_요청인증.md`
- `06_인증_RefreshToken_재발급과_세션Rotation.md`
- `07_인증_로그아웃_Cookie_Origin_CORS.md`
- `08_인증_세션정리_요청로그_테스트.md`
- 이 문서: `99_작업인수인계_다음AI용.md`

---

## 2. 사용자의 최종 학습 목표

사용자는 AI가 작성한 코드가 많아 주석 없는 실제 코드를 처음부터 끝까지 이해하고 싶어 합니다. 목표는 모든 파일을 기계적으로 읽는 것이 아니라, 다음 조건을 만족하는 것입니다.

1. 애플리케이션이 시작되는 시점부터 HTTP 요청이 Controller, Service, Repository, Entity, DB·Redis를 거쳐 응답으로 돌아오는 전체 흐름을 이해한다.
2. 프론트엔드에서 사용자의 동작이 화면 state, API module, 공통 request, 백엔드 endpoint와 어떻게 연결되는지 이해한다.
3. 새로운 문법·annotation·라이브러리 API가 처음 등장하면 반드시 그 코드 위치에서 설명한다.
4. 완전히 중복되는 코드만 실제 사용 위치와 역할을 확인한 뒤 스킵한다.
5. 각 메서드와 변수의 선언 파일, 호출 위치, 매개변수 출처, 반환값 사용 위치, 예외 이동 경로를 추적한다.
6. 코드를 작성하는 순서와 프로그램이 실행되는 순서를 구분한다.
7. 설명은 실제 현재 저장소 코드와 검색 결과에 근거한다. 확인하지 못한 내용은 추측하지 않고 `현재 코드만으로 확인할 수 없음`이라고 표시한다.

---

## 3. 반드시 지켜야 하는 정확성 규칙

다음 AI는 설명을 빨리 완성하는 것보다 실제 코드와의 일치를 우선해야 합니다.

- 실제 파일을 열고 원문을 확인한 뒤 설명한다.
- `rg` 등으로 import, route, 문자열 참조, 메서드 호출, workflow 참조를 검색한다.
- Spring annotation 때문에 연결되는 관계는 단순 import 검색만으로 결론내리지 않는다.
- 문서의 문장을 현재 구현, 과거 코드·장애, 테스트로 증명된 내용, 사용자의 의도, 설정 설명으로 구분한다.
- 과거 코드 설명은 현재 코드와 섞지 않는다. 기존 내용이 남아 있다면 `변경 전 참고`와 `현재 코드`를 분리한다.
- 실제로 실행하지 않은 테스트를 실행했다고 쓰지 않는다.
- 파일을 수정하기 전에는 변경 범위·방법·검증 방법을 먼저 설명하고, 사용자의 명시적 승인을 받은 뒤 수정한다.
- 문서만 수정할 때도 실제 코드와 문서를 다시 대조한다.
- 기존 설명이 틀렸다면 방어하지 말고 `무엇이 틀렸는지 → 왜 틀렸는지 → 현재 결론` 순서로 바로잡는다.

---

## 4. 문서 작성 형식: 현재 최종 규칙

대화 중 형식 요구가 여러 번 바뀌었으므로, 아래 규칙을 최종 규칙으로 사용합니다.

### 4.1 연관된 파일은 한 문서에 묶는다

호출 흐름상 함께 읽어야 하는 파일은 파일별로 분리하지 말고 하나의 Markdown 문서 안에서 실제 실행 순서대로 이어 붙입니다.

예시:

`JwtAuthenticationFilter.java → CustomUserDetailsService.java → CustomUserDetails.java`

인증 문서는 실행 흐름별 5개 Markdown으로 나누며, 각 문서 내부에서도 파일별 코드 블록을 따로 둡니다. 현재 구조에서 인증 파일을 다시 하나의 Markdown으로 합치지 않습니다. 다섯 문서가 각각 로그인, Access Token 요청 인증, Refresh Token rotation, 로그아웃·Origin/CORS, 세션 정리·테스트를 담당합니다. 실행 기반·회원가입·이미지 검증·예외 응답은 인증 문서와 섞지 않고 각각 `01`, `02`, `03` 문서에 둡니다.

기존 인증 문서 9개는 내용 손실을 막기 위해 `project-study/auth-legacy-backup`에 보관합니다. 이 디렉터리는 현재 학습 순서에 포함하지 않는 복구용 사본입니다.

### 4.2 코드 블록은 파일별로 하나씩 둔다

과거에는 `전체 주석본`과 `목차 주석본`을 따로 만들거나 여러 파일을 하나의 거대한 코드 블록에 섞는 문제가 있었습니다. 현재 최종 규칙은 **파일 하나당 전체 코드 블록 하나**입니다.

연관된 여러 파일을 하나의 Markdown에 묶을 수는 있지만, 코드 블록까지 하나로 합치지 않습니다.

각 파일은 반드시 다음 순서입니다.

해당 파일의 전체 코드
→ 설명할 코드 일부
→ 그 코드 일부에 대한 설명
→ 다음 코드 일부
→ 그 코드 일부에 대한 설명
→ 해당 파일의 설명이 끝난 뒤 다음 파일의 전체 코드

한 파일의 코드와 설명이 끝나기 전에 다음 파일의 코드나 설명을 넣지 않습니다. 설명 앞에는 반드시 설명 대상 코드 일부를 다시 보여주며, 번호로만 위치를 표시하지 않습니다.

하나의 파일 코드 블록에는 다음을 함께 넣습니다.

- 실제 코드의 원문 순서
- 모든 중요한 줄의 `//` 설명 주석
- 호출 위치·값의 출처·반환값의 도착 위치·예외 이동 경로
- 처음 등장하는 문법과 현재 코드에서 사용하는 이유

`package`와 `import` 줄에는 주석을 달지 않습니다.

### 4.3 실제 코드 생략 금지

사용자는 코드 일부만 보면 흐름이 끊긴다고 여러 번 지적했습니다. 따라서 전체 파일을 설명하는 문서에서는 원문을 생략하지 않습니다.

정말로 연결 확인만 필요한 파일에서 발췌할 때만 다음 정보를 적습니다.

- `발췌`라는 표시
- 실제 절대 경로
- 발췌한 메서드명과 줄 범위
- 전체 파일을 넣지 않은 이유
- 생략된 나머지 코드가 흐름 이해에 필요 없는 근거

### 4.4 번호 주석 배치

번호는 설명 위치를 찾기 위한 목차 역할입니다. 번호만 멀리 떨어진 곳에 모아두면 안 됩니다.

- `/* 1. 클래스의 책임과 호출 위치 */`처럼 코드 묶음 바로 위에 둔다.
- 메서드 내부의 세부 줄은 `// 1` 또는 `//1,3`처럼 해당 줄 가까이에 표시한다.
- 여러 설명이 같은 코드 구간에 적용되면 병합 번호를 유지한다.
- 코드 한 줄에서 시작한 설명을 문서 아래 먼 위치로 보내지 않는다.
- 한 코드에 번호가 너무 많이 걸리면 메서드·생성자·조건문·반복문 단위로 코드 구간을 나눈다.

### 4.5 모든 설명은 “무엇”과 “왜”를 함께 적는다

결과만 적지 않습니다. 최소한 다음 질문에 답해야 합니다.

- 이 코드의 책임은 무엇인가?
- 누가 이 코드를 호출하는가?
- 언제 실행되는가? 애플리케이션 시작 시인가, 요청이 들어왔을 때인가?
- 매개변수는 어디에서 왔는가?
- 내부에서 어떤 순서로 값이 변하는가?
- 반환값은 어느 파일의 어느 메서드에서 사용하는가?
- 예외가 발생하면 어느 Handler·Filter·호출자에게 이동하는가?
- DB·Redis·HTTP response·SecurityContext 중 무엇을 변경하는가?
- 이 구현을 사용한 이유와 사용하지 않았을 때의 차이는 무엇인가?

Spring이 내부에서 이어서 처리하는 경우에는 반드시 처리 주체를 적습니다. 예를 들면 `DispatcherServlet`, `Jackson`, `HttpServletResponse`, `SecurityFilterChain`, `SecurityContextHolder` 등이 있습니다.

### 4.6 새로운 문법은 처음 나오는 위치에서 설명한다

문법 설명을 문서 마지막에 모으지 않습니다.

처음 등장하는 코드 바로 아래에 다음을 적습니다.

- 문법의 의미
- 이 프로젝트 코드에서 왜 사용했는지
- 현재 입력과 반환값으로 실행 결과가 무엇인지
- 짧은 일반 예시

같은 문법이 반복되면 `앞의 파일에서 설명한 문법`이라고 표시하고, 현재 코드에서 달라지는 점만 설명합니다. 단, 사용자가 다시 질문한 개념은 이미 설명했다는 이유로 스킵하지 않고 해당 코드 위치에서 다시 설명합니다.

---

## 5. 지금까지 확정된 주요 요구사항

다음은 대화에서 사용자가 반복해서 확정한 요구입니다.

- 실제 코드 원문을 먼저 보여주고 설명한다.
- 축약 코드만 보여주지 않는다.
- 프론트 코드도 주석을 생략하지 않는다.
- `errors`처럼 코드에 실제로 존재하는 state·field는 빠뜨리지 않는다.
- 코드의 모든 줄이 어느 설명을 가리키는지 코드 가까이에 표시한다.
- 새로운 문법과 개념은 설명용 주석 코드 안에 넣는다.
- 다른 코드와 연결되어 있으면 이해에 필요한 관련 코드도 함께 발췌한다.
- 관련 파일의 호출 관계, 값 전달, 반환값 사용 위치, 예외 경로를 반드시 연결한다.
- 지금 당장 호출되지 않는 상수나 Bean도 어디에서 쓰이는지, 현재 흐름에서 안 쓰이면 그 사실을 적는다.
- 같은 개념이 다른 화면이나 기능에서 다시 나오면 그 위치에서도 흐름상 필요한 설명을 반복한다.
- 완전히 중복된 파일만 역할과 차이를 확인한 뒤 스킵한다.
- 파일 하나를 완성할 때마다 파일 개수 기준 진행률을 표시한다.
- 사용자가 질문한 내용은 기존 설명이 부족했다는 뜻으로 보고, 이후 해당 코드 문서에 더 자세히 반영한다.
- 설명을 추가할 때 기존의 유효한 설명을 삭제하지 않는다.

---

## 6. 처음 세운 전체 학습 계획

아래 순서는 폴더 순서가 아니라 실행 흐름과 의존 관계에 따른 학습 순서입니다.

### 0단계. 실행 기반

1. `build.gradle`
2. `settings.gradle`
3. `application.yaml`
4. `application-local.yaml`
5. `application-prod.yaml`
6. `SpringdatajpaApplication.java`

build와 profile은 짧게 확인하되, 첫 번째 실제 Java 코드 정독은 `SpringdatajpaApplication.java`에서 시작합니다.

### 1단계. 회원가입

1. `User.java`
2. user Request DTO
3. user Response DTO
4. `UserRepository.java`
5. `UserService.java`
6. `UserController.java`
7. `GlobalExceptionHandler.java`
8. `ErrorResponseDto.java`

처음 설명할 개념: Java class, annotation, Lombok, JPA Entity, DTO, Repository, Service, Controller, validation, exception, JSON response.

### 2단계. 검증·예외 세부 흐름

1. `ImageDataUrlValidator.java`
2. `InvalidRequestException.java`
3. `DataNullException.java`
4. `ForbiddenException.java`
5. `CounterUpdateException.java`
6. `PostVersionConflictException.java`
7. `LoginFailedException.java`
8. `AuthException.java`

첫 예외 class에서 `RuntimeException` 상속과 생성자를 자세히 설명하고, 반복 예외는 실제 사용 위치와 Handler의 status를 확인합니다.

### 3단계. 로그인·인증

1. Session DTO
2. `JwtProvider.java`
3. `RefreshTokenProvider.java`
4. `RefreshCookieProvider.java`
5. `AuthSession.java`
6. `AuthSessionRepository.java`
7. `CustomUserDetails.java`
8. `CustomUserDetailsService.java`
9. `JwtAuthenticationFilter.java`
10. `SessionService.java`
11. `SessionController.java`
12. `SecurityConfig.java`
13. `SessionOriginInterceptor.java`
14. `InterceptorConfig.java`
15. `AuthSessionCleanupScheduler.java`

현재 3단계의 대부분은 문서화했지만, 인증 파일 전체를 하나의 실행 순서 문서로 합치는 작업은 아직 남아 있습니다.

### 4단계. 게시글 Entity와 API

1. `Post.java`
2. `PostCounter.java`
3. `PostImage.java`
4. `Comment.java`
5. `Like.java`
6. `PostReport.java`
7. `PostViewCount.java`
8. 게시글 DTO
9. 게시글 Repository
10. `PostService.java`
11. `CommentService.java`
12. `PostController.java`
13. `CommentController.java`

### 5단계. 프론트 실행과 인증 화면

1. `main.jsx`
2. `App.jsx`
3. `AppRoutes.jsx`
4. `ProtectedRoute.jsx`
5. `PublicOnlyRoute.jsx`
6. `PageBoundary.jsx`
7. `authStorage.js`
8. `api.js`
9. `authApi.js`
10. `LoginForm.jsx`
11. `LoginPage.jsx`
12. `SignupForm.jsx`
13. `SignupPage.jsx`

### 6단계. 게시글 목록·페이징

1. `postApi.js`
2. `postNormalizer.js`
3. `PostsPage.jsx`
4. `PostBoard.jsx`
5. `PostList.jsx`
6. `PostListItem.jsx`
7. 목록 표시 컴포넌트

처음 설명할 개념: `useState`, `useEffect`, `useRef`, `useCallback`, `AbortController`, `IntersectionObserver`, Spring `Page<T>`, `Pageable`.

### 7단계. 게시글 상세·작성·수정·댓글·회원정보

`PostDetailPage.jsx`, 상세 컴포넌트, `PostCreatePage.jsx`, `PostEditPage.jsx`, `PostEditor.jsx`, 이미지 선택 컴포넌트, 댓글 컴포넌트, `userApi.js`, 회원정보 form, 비밀번호 변경 form 순서입니다.

### 8단계. Redis 조회수

1. `ViewCountProperties.java`
2. `ViewCountUpdater.java`
3. `DatabaseViewCountUpdater.java`
4. `RedisViewCountStore.java`
5. `RedisViewCountFlushScheduler.java`
6. `ViewCountPersistenceService.java`
7. `PostViewCountRepository.java`
8. `AuthSessionCleanupScheduler.java`

### 9단계. Flyway·RDS

1. `B3__current_schema.sql`
2. `V1__migrate_post_counters.sql`
3. `V2__add_user_auth_version.sql`
4. `V3__expand_post_view_counts.sql`
5. 실제 현재 저장소에 존재하는 `V4__remove_legacy_post_counter_view_count.sql`
6. `FlywayBaselineMigrationTest.java`
7. `MySqlSchemaIntegrationTest.java`

초기 계획에는 V4가 빠져 있었으므로, 다음 AI는 반드시 현재 저장소의 V4를 실제로 확인해야 합니다.

### 10단계. 테스트

1. Spring Boot context test
2. Entity 단위 테스트
3. JWT·Refresh Provider 테스트
4. Service Mockito 테스트
5. Controller 위임 테스트
6. Security MockMvc 테스트
7. H2 Repository 통합 테스트
8. MySQL 통합 테스트
9. Redis mock 테스트
10. Redis Testcontainers 테스트
11. Redis AOF 재시작 테스트
12. 동시성 테스트

### 11단계. 배포

1. 백엔드 Dockerfile
2. 프론트 Dockerfile
3. 각 프로젝트의 `deploy/.env.example`
4. 백엔드 Compose
5. 프론트 Compose
6. Nginx 설정
7. 배포 Shell script
8. GitHub Actions workflow

---

## 7. 실제 파일 전체 목록

이 목록은 작성 시점에 `rg --files`로 확인한 파일입니다. `node_modules`, 빌드 결과물, `.git` 내부는 학습 대상에서 제외합니다.

### 7.1 백엔드 루트·빌드·배포

- `build.gradle`
- `settings.gradle`
- `gradlew`
- `gradlew.bat`
- `gradle/wrapper/gradle-wrapper.jar`
- `gradle/wrapper/gradle-wrapper.properties`
- `Dockerfile`
- `README.md`
- `image.png` — 시각 자료이며 React/Spring 실행 흐름을 이해하는 새 문법이 없으면 스킵한다.
- `deploy/.env.example`
- `deploy/compose.yaml`
- `deploy/deploy-backend.sh`
- `deploy/nginx/backend-blue.conf`
- `deploy/nginx/backend-green.conf`
- `.github/workflows/backend-ci.yml`

### 7.2 백엔드 Java 시작·설정

경로 기준: `/Users/miles/Documents/GitHub/KTB4_Miles_Week12_Back/src/main/java/kr/adapterz/springdatajpa`

- `SpringdatajpaApplication.java`
- `config/CorsOriginProvider.java`
- `config/ErrorResponseWriter.java`
- `config/InterceptorConfig.java`
- `config/RequestLogInterceptor.java`
- `config/SecurityConfig.java`
- `config/SessionOriginInterceptor.java`
- `config/ViewCountProperties.java`
- `config/WebConfig.java`

`ErrorResponseWriter.java`는 초기 계획 목록에 빠져 있었지만 현재 코드에서 `SecurityConfig`, `JwtAuthenticationFilter`, `SessionOriginInterceptor`가 사용하므로 반드시 읽어야 합니다.

### 7.3 백엔드 인증

- `auth/AccessTokenClaims.java`
- `auth/CustomUserDetails.java`
- `auth/CustomUserDetailsService.java`
- `auth/JwtAuthenticationFilter.java`
- `auth/JwtProvider.java`
- `auth/RefreshCookieProvider.java`
- `auth/RefreshTokenProvider.java`
- `entity/AuthSession.java`
- `repository/AuthSessionRepository.java`
- `service/AuthSessionCleanupScheduler.java`
- `service/SessionService.java`
- `controller/SessionController.java`

### 7.4 백엔드 Entity

- `entity/User.java`
- `entity/Post.java`
- `entity/PostCounter.java`
- `entity/PostImage.java`
- `entity/Comment.java`
- `entity/Like.java`
- `entity/PostReport.java`
- `entity/PostViewCount.java`

### 7.5 백엔드 DTO

공통:

- `dto/ErrorResponseDto.java`

user:

- `dto/user/SessionRefreshResponseDto.java`
- `dto/user/SessionRequestDto.java`
- `dto/user/SessionResponseDto.java`
- `dto/user/UserDeleteResponseDto.java`
- `dto/user/UserInfoResponseDto.java`
- `dto/user/UserPasswordRequestDto.java`
- `dto/user/UserPasswordResponseDto.java`
- `dto/user/UserPatchRequestDto.java`
- `dto/user/UserPatchResponseDto.java`
- `dto/user/UserRequestDto.java`
- `dto/user/UserResponseDto.java`

post:

- `dto/post/LikeCancelResponseDto.java`
- `dto/post/LikeResponseDto.java`
- `dto/post/PostDeleteRequestDto.java`
- `dto/post/PostDeleteResponseDto.java`
- `dto/post/PostFixRequestDto.java`
- `dto/post/PostFixResponseDto.java`
- `dto/post/PostListResponseDto.java`
- `dto/post/PostPageResponseDto.java`
- `dto/post/PostReportResponseDto.java`
- `dto/post/PostRequestDto.java`
- `dto/post/PostResponseDto.java`
- `dto/post/PostResponseFactory.java`
- `dto/post/PostViewResponseDto.java`
- `dto/post/ReportResponseDto.java`

comment:

- `dto/comment/CommentDeleteRequestDto.java`
- `dto/comment/CommentDeleteResponseDto.java`
- `dto/comment/CommentFixRequestDto.java`
- `dto/comment/CommentPostRequestDto.java`
- `dto/comment/CommentResponseDto.java`

### 7.6 백엔드 Repository·Service·예외

Repository:

- `repository/UserRepository.java`
- `repository/PostRepository.java`
- `repository/PostCounterRepository.java`
- `repository/PostViewCountRepository.java`
- `repository/CommentRepository.java`
- `repository/LikeRepository.java`
- `repository/PostReportRepository.java`
- `repository/AuthSessionRepository.java`

Service·검증·조회수:

- `service/UserService.java`
- `service/PostService.java`
- `service/CommentService.java`
- `service/RedisViewCountStore.java`
- `service/RedisViewCountFlushScheduler.java`
- `service/DatabaseViewCountUpdater.java`
- `service/ViewCountUpdater.java`
- `service/ViewCountPersistenceService.java`
- `validation/ImageDataUrlValidator.java`

Controller·예외:

- `controller/UserController.java`
- `controller/SessionController.java`
- `controller/PostController.java`
- `controller/CommentController.java`
- `controller/GlobalExceptionHandler.java`
- `exception/ApiErrorCode.java`
- `exception/AuthException.java`
- `exception/CounterUpdateException.java`
- `exception/DataNullException.java`
- `exception/ForbiddenException.java`
- `exception/InvalidRequestException.java`
- `exception/LoginFailedException.java`
- `exception/PostVersionConflictException.java`

### 7.7 백엔드 리소스·migration

- `src/main/resources/application.yaml`
- `src/main/resources/application-local.yaml`
- `src/main/resources/application-prod.yaml`
- `src/main/resources/data.sql`
- `src/main/resources/db/migration/B3__current_schema.sql`
- `src/main/resources/db/migration/V1__migrate_post_counters.sql`
- `src/main/resources/db/migration/V2__add_user_auth_version.sql`
- `src/main/resources/db/migration/V3__expand_post_view_counts.sql`
- `src/main/resources/db/migration/V4__remove_legacy_post_counter_view_count.sql`

### 7.8 백엔드 테스트

- `src/test/java/kr/adapterz/springdatajpa/SpringdatajpaApplicationTests.java`
- `src/test/java/kr/adapterz/springdatajpa/auth/CustomUserDetailsServiceTest.java`
- `src/test/java/kr/adapterz/springdatajpa/auth/JwtAuthenticationFilterTest.java`
- `src/test/java/kr/adapterz/springdatajpa/auth/JwtProviderTest.java`
- `src/test/java/kr/adapterz/springdatajpa/auth/RefreshTokenProviderTest.java`
- `src/test/java/kr/adapterz/springdatajpa/config/FlywayBaselineMigrationTest.java`
- `src/test/java/kr/adapterz/springdatajpa/config/MySqlSchemaIntegrationTest.java`
- `src/test/java/kr/adapterz/springdatajpa/config/SecurityConfigTest.java`
- `src/test/java/kr/adapterz/springdatajpa/config/ViewCountPropertiesTest.java`
- `src/test/java/kr/adapterz/springdatajpa/controller/ControllerDelegationTest.java`
- `src/test/java/kr/adapterz/springdatajpa/dto/post/PostResponseFactoryTest.java`
- `src/test/java/kr/adapterz/springdatajpa/entity/AuthSessionTest.java`
- `src/test/java/kr/adapterz/springdatajpa/entity/CommentTest.java`
- `src/test/java/kr/adapterz/springdatajpa/entity/PostCounterTest.java`
- `src/test/java/kr/adapterz/springdatajpa/entity/PostImageTest.java`
- `src/test/java/kr/adapterz/springdatajpa/entity/PostReportTest.java`
- `src/test/java/kr/adapterz/springdatajpa/entity/PostTest.java`
- `src/test/java/kr/adapterz/springdatajpa/entity/PostViewCountTest.java`
- `src/test/java/kr/adapterz/springdatajpa/entity/UserTest.java`
- `src/test/java/kr/adapterz/springdatajpa/repository/PostRepositoryIntegrationTest.java`
- `src/test/java/kr/adapterz/springdatajpa/repository/PostViewCountRepositoryIntegrationTest.java`
- `src/test/java/kr/adapterz/springdatajpa/repository/UserSoftDeleteIntegrationTest.java`
- `src/test/java/kr/adapterz/springdatajpa/service/CommentServiceTest.java`
- `src/test/java/kr/adapterz/springdatajpa/service/PostConcurrencyIntegrationTest.java`
- `src/test/java/kr/adapterz/springdatajpa/service/PostServiceTest.java`
- `src/test/java/kr/adapterz/springdatajpa/service/RedisAofRestartIntegrationTest.java`
- `src/test/java/kr/adapterz/springdatajpa/service/RedisViewCountFlushSchedulerTest.java`
- `src/test/java/kr/adapterz/springdatajpa/service/RedisViewCountStoreIntegrationTest.java`
- `src/test/java/kr/adapterz/springdatajpa/service/RedisViewCountStoreTest.java`
- `src/test/java/kr/adapterz/springdatajpa/service/SessionServiceTest.java`
- `src/test/java/kr/adapterz/springdatajpa/service/UserServiceTest.java`
- `src/test/resources/application-test.yaml`

### 7.9 프론트 루트·실행·라우팅

프론트 루트:

- `package.json`
- `package-lock.json`
- `vite.config.js`
- `index.html`
- `Dockerfile`
- `nginx/default.conf`
- `README.md`

실행·라우팅:

- `src/main.jsx`
- `src/App.jsx`
- `src/routes/AppRoutes.jsx`
- `src/routes/ProtectedRoute.jsx`
- `src/routes/PublicOnlyRoute.jsx`
- `src/components/layout/PageBoundary.jsx`

API·인증:

- `src/api/api.js`
- `src/api/authApi.js`
- `src/api/postApi.js`
- `src/api/postNormalizer.js`
- `src/api/userApi.js`
- `src/auth/authStorage.js`

페이지:

- `src/pages/auth/LoginPage.jsx`
- `src/pages/auth/SignupPage.jsx`
- `src/pages/posts/PostCreatePage.jsx`
- `src/pages/posts/PostDetailPage.jsx`
- `src/pages/posts/PostEditPage.jsx`
- `src/pages/posts/PostsPage.jsx`
- `src/pages/user/PasswordEditPage.jsx`
- `src/pages/user/ProfileEditPage.jsx`

컴포넌트:

- auth: `LoginForm.jsx`, `SignupForm.jsx`
- common: `ActionButton.jsx`, `AppLink.jsx`, `ConfirmModal.jsx`, `ErrorView.jsx`, `FormField.jsx`, `LoadingView.jsx`, `TextArea.jsx`, `TextInput.jsx`, `Toast.jsx`
- layout: `AppHeader.jsx`, `AuthIntroPanel.jsx`, `AuthLayout.jsx`, `InfoBanner.jsx`, `PageBoundary.jsx`, `PageLayout.jsx`
- posts: `ActivityBars.jsx`, `AuthorSummary.jsx`, `CommentEditor.jsx`, `CommentItem.jsx`, `CommentList.jsx`, `CommentSection.jsx`, `PostBoard.jsx`, `PostDetailCard.jsx`, `PostEditor.jsx`, `PostImageGallery.jsx`, `PostImagePicker.jsx`, `PostLikeButton.jsx`, `PostList.jsx`, `PostListItem.jsx`, `PostMetricCard.jsx`, `PostReportButton.jsx`
- user: `PasswordEditForm.jsx`, `ProfileEditForm.jsx`, `ProfileImagePicker.jsx`
- utils: `errorMessage.js`, `file.js`, `formatter.js`, `validation.js`

시각 asset:

- `src/assets/fonts/*`
- `src/assets/images/*`
- `src/styles/auth.css`
- `src/styles/common.css`
- `src/styles/posts.css`
- `src/styles/user.css`

CSS, font, SVG는 import 위치와 화면 역할만 확인하고, 새 실행 문법이 없으면 정독하지 않습니다.

### 7.10 프론트 배포

- `deploy/.env.example`
- `deploy/compose.yaml`
- `deploy/deploy-frontend.sh`
- `deploy/nginx/frontend-blue.conf.template`
- `deploy/nginx/frontend-green.conf.template`
- `.github/workflows/frontend-ci.yml`

---

## 8. 현재 문서별 진행 상태와 포함 파일

진행률은 장 수가 아니라 **중복 제거한 실제 파일 확인 수**로 계산합니다. 현재 공식 기록은 `41개 확인 완료 / 최소 학습 대상 213개 ≈ 19.2%`입니다. 이 숫자는 문서가 늘어났다는 이유만으로 올리지 말고, 실제 원문 정독·호출 검색·반환값 확인이 끝난 파일만 추가합니다.

### `01_실행기반_build_profile_main.md`

완료된 파일:

- `build.gradle`
- `settings.gradle`
- `application.yaml`
- `application-local.yaml`
- `application-prod.yaml`
- `SpringdatajpaApplication.java`

확인한 핵심: Gradle plugin·dependency, profile YAML, Spring Boot main, `@ConfigurationPropertiesScan`, `@EnableScheduling`.

### `02_회원가입_실제흐름.md`

완료 또는 전체 흐름에 포함된 파일:

- `User.java`
- `UserRequestDto.java`
- `UserResponseDto.java`
- `UserRepository.java`
- `UserService.java`
- `UserController.java`
- `SignupPage.jsx`
- `authApi.js`와 `api.js`는 회원가입 호출 연결에 필요한 코드가 발췌되어 있음. 전체 정독 파일로 계산할 때는 다시 확인해야 함.

중요한 프론트 state:

- `form`: email, password, passwordCheck, nickname 등의 입력값
- `errors`: field별 검증·서버 오류 문구
- `isValid`: 현재 form의 프론트 검증 결과
- `handleBlur`: field별 검증 실행
- `handleSubmit`: 프론트 검증 → `authApi.signup` → 성공 시 이동 → 실패 시 errors 갱신

`errors`는 별도 객체이며 실제 구조는 `email`, `password`, `passwordCheck`, `nickname` key를 사용합니다. 값이 없을 때는 없거나 빈 문자열일 수 있으므로 실제 현재 코드를 다시 열어 확인한 뒤 설명합니다.

### `03_이미지검증과_예외응답.md`

포함 파일:

- `ImageDataUrlValidator.java`
- `InvalidRequestException.java`
- `DataNullException.java`
- `ForbiddenException.java`
- `CounterUpdateException.java`
- `PostVersionConflictException.java`
- `LoginFailedException.java`
- `AuthException.java`
- `GlobalExceptionHandler.java`
- `ApiErrorCode.java`
- `ErrorResponseDto.java`
- `ErrorResponseWriter.java`
- 프론트 `errorMessage.js`, `api.js`와 Filter·Interceptor 연결 발췌

주의: 이 문서에는 변경 전 설명을 보존한 부분과 최신 `ApiErrorCode`·`ErrorResponseWriter` 기준 개정 부분이 함께 있습니다. 다음 AI는 `개정판` 또는 `현재 코드`라고 표시된 구간을 우선하고, 문장을 현재 코드와 다시 대조해야 합니다. 기존 설명을 무조건 현재 구현으로 간주하지 않습니다.

### `04_인증_로그인_자격증명과_세션생성.md`

포함 파일: `SessionRequestDto.java`, `SessionResponseDto.java`, `JwtProvider.java`, `CustomUserDetailsService.java`, `CustomUserDetails.java`, `SessionService.java`, `SessionController.java`.

핵심 흐름: 로그인 DTO → AuthenticationManager → 사용자 조회·비밀번호 검증 → Access Token·Refresh Session 생성 → Cookie/body 응답.

### `05_인증_AccessToken_요청인증.md`

포함 파일: `AccessTokenClaims.java`, `SecurityConfig.java`, `JwtAuthenticationFilter.java`.

핵심 흐름: Authorization Bearer → JWT 검증 → 활성 사용자 재조회 → SecurityContext → 보호 endpoint 인가.

### `06_인증_RefreshToken_재발급과_세션Rotation.md`

포함 파일: `RefreshTokenProvider.java`, `RefreshCookieProvider.java`, `SessionRefreshResponseDto.java`, `AuthSession.java`, `AuthSessionRepository.java`.

핵심 흐름: Cookie → hash → row lock → 활성 상태 확인 → rotate → 새 Access Token/Cookie → 프론트 재시도.

### `07_인증_로그아웃_Cookie_Origin_CORS.md`

포함 파일: `CorsOriginProvider.java`, `WebConfig.java`, `SessionOriginInterceptor.java`, `InterceptorConfig.java`와 로그아웃 연결 코드.

핵심 흐름: DB session revoke + Cookie 만료 + CORS 등록 + 세션 endpoint Origin 차단.

### `08_인증_세션정리_요청로그_테스트.md`

포함 파일: `RequestLogInterceptor.java`, `AuthSessionCleanupScheduler.java` 및 인증 테스트 연결.

핵심 흐름: MVC 요청 관찰 + 만료 AuthSession 정리 + 인증 테스트 실행 범위.

## 9. 현재 확인된 핵심 실행 흐름

아래는 시작 지도입니다. 다음 AI는 각 장에서 실제 코드와 검색 결과를 다시 연결해 설명해야 합니다.

### 9.1 애플리케이션 시작

```text
SpringdatajpaApplication.main
→ SpringApplication.run
→ @SpringBootApplication component scan
→ YAML profile 병합
→ ConfigurationProperties Bean 생성
→ SecurityConfig·WebConfig·InterceptorConfig Bean 생성
→ Repository/Service/Controller Bean 생성
→ @EnableScheduling으로 Scheduler 등록
```

`@Bean` 메서드가 애플리케이션 시작 중 호출되어 객체를 준비한다는 사실과, 그 객체의 업무 메서드가 요청 시 실행된다는 사실을 구분해야 합니다.

### 9.2 회원가입

```text
SignupPage.handleSubmit
→ authApi.signup
→ api.js request
→ fetch POST /users
→ UserController.createUser
→ @RequestBody 역직렬화·@Valid 검증
→ UserService.createUser
→ 중복·업무 규칙 검사
→ ImageDataUrlValidator 호출
→ PasswordEncoder.encode
→ User Entity 생성·저장
→ UserResponseDto
→ Jackson JSON body
→ 프론트 성공 또는 errorMessage 처리
```

프론트 validation은 사용자 경험을 위한 사전 검사이고, 백엔드 `@Valid`와 Service 검사는 최종 검증입니다. 프론트가 통과해도 서버 검증은 반드시 별도로 실행됩니다.

### 9.3 로그인

```text
LoginForm 제출
→ authApi 로그인 함수
→ api.js request
→ POST /sessions
→ SessionController.createSession
→ SessionService.createSession
→ AuthenticationManager.authenticate
→ CustomUserDetailsService.loadUserByUsername
→ UserRepository.findByEmailAndDeletedFalse
→ 저장된 password hash와 입력 password 비교
→ Authentication 반환
→ CustomUserDetails에서 userId/authVersion 추출
→ JwtProvider.createAccessToken
→ RefreshTokenProvider.createRefreshToken
→ RefreshTokenProvider.hashRefreshToken
→ AuthSession 생성·AuthSessionRepository.save
→ SessionResponseDto 반환
→ Controller가 Set-Cookie header 추가
→ Jackson이 body 직렬화
```

#### AuthenticationManager의 정확한 실행 시점

현재 코드에는 다음 두 시점을 반드시 구분해야 합니다.

1. `SecurityConfig.authenticationManager(...)`의 `@Bean` 메서드: Spring이 애플리케이션 Context를 만들 때 `AuthenticationManager` 객체를 준비하고 `SessionService`에 주입합니다. 이때 사용자의 이메일·비밀번호 검증은 실행되지 않습니다.
2. `SessionService.createSession()` 안의 `authenticationManager.authenticate(...)`: 실제 `POST /sessions` 로그인 요청이 들어왔을 때 명시적으로 실행됩니다. 이때 사용자 조회와 비밀번호 검증이 일어나고, 성공한 `Authentication`이 다음 코드로 반환됩니다.

따라서 `AuthenticationManager` 객체는 요청 전에 준비되어 있지만, `authenticate()` 업무 실행은 로그인 요청 시점입니다. “Bean이 자동 실행된다”라고 설명하면 안 됩니다.

### 9.4 Access Token 인증

```text
보호된 프론트 API request
→ api.js가 Authorization: Bearer <token> 추가
→ SecurityFilterChain
→ JwtAuthenticationFilter
→ shouldNotFilter 대상인지 확인
→ JwtProvider.getAccessTokenClaims
→ 서명·claim 검증
→ CustomUserDetailsService.loadUserByUserId
→ UserRepository에서 활성 User 조회
→ deleted·suspended·authVersion 확인
→ UsernamePasswordAuthenticationToken 생성
→ SecurityContextHolder에 Authentication 저장
→ 다음 Filter와 Controller로 진행
```

로그인 시 `AuthenticationManager`가 이메일·비밀번호를 검증하는 흐름과, 이후 요청에서 `JwtAuthenticationFilter`가 JWT를 검증하는 흐름은 서로 다릅니다.

### 9.5 Refresh Token 재발급

```text
프론트 API가 401 수신
→ api.js의 refreshPromise 공유 로직
→ POST /sessions/refresh
→ Cookie에서 refresh token 추출
→ SessionController.refreshSession
→ SessionService.refreshSession
→ 원문 token hash 계산
→ AuthSessionRepository.findByRefreshTokenHash
→ PESSIMISTIC_WRITE row lock
→ isActive·User 상태 확인
→ 새 원문 token·hash 생성
→ 같은 AuthSession.rotate
→ 새 Access Token 생성
→ 새 Set-Cookie header와 body 반환
→ 원래 API 요청 재시도
```

동시 Refresh 요청의 정확한 결과는 실제 lock·transaction·DB 제약과 테스트를 함께 확인해야 합니다. 근거 없이 “항상 특정 예외가 된다”고 단정하지 않습니다.

### 9.6 게시글 목록·페이징

```text
PostsPage 렌더링
→ postApi 목록 함수
→ api.js request
→ PostController 목록 endpoint
→ PostService 목록 메서드
→ PostRepository Page query
→ Pageable이 page/size/sort 전달
→ Page 또는 PostPageResponseDto
→ JSON 응답
→ PostsPage state 갱신
→ PostList 렌더링
→ IntersectionObserver 또는 현재 코드의 다음 페이지 조건
```

실제 프론트 목록 로딩·페이징 코드는 아직 전체 정독하지 않았습니다. 다음 AI는 `PostsPage.jsx`, `postApi.js`, `PostBoard.jsx`, `PostList.jsx`를 실제 검색해 연결해야 합니다.

### 9.7 조회수·Redis fallback

```text
게시글 상세 진입
→ PostService 또는 조회수 호출 위치
→ ViewCountUpdater
→ RedisViewCountStore key 증가·dirty 기록
→ RedisViewCountFlushScheduler
→ ViewCountPersistenceService
→ DatabaseViewCountUpdater
→ PostViewCountRepository
```

Redis 장애 시 실제 fallback과 실패 처리 범위는 해당 Service·테스트를 정독한 뒤 확정해야 합니다.

### 9.8 배포

```text
GitHub Actions
→ build/test
→ Docker image build·push
→ AWS/OIDC·SSM 단계
→ deploy script
→ Compose service 실행
→ health 확인
→ Nginx blue/green upstream 전환
```

배포 파일은 아직 정독하지 않았으므로 포트·환경변수·health 명령·전환 방식은 실제 파일을 확인하기 전까지 확정하지 않습니다.

---

## 10. 현재 코드에서 확인된 의존 관계

```text
SpringdatajpaApplication
├─ @ConfigurationPropertiesScan
│  └─ ViewCountProperties
├─ @EnableScheduling
│  ├─ RedisViewCountFlushScheduler
│  └─ AuthSessionCleanupScheduler
└─ @SpringBootApplication component scan

UserController
└─ UserService
   ├─ UserRepository
   ├─ PasswordEncoder
   └─ ImageDataUrlValidator

SessionController
└─ SessionService
   ├─ AuthenticationManager
   ├─ JwtProvider
   ├─ RefreshTokenProvider
   └─ AuthSessionRepository

SecurityConfig
├─ JwtAuthenticationFilter
├─ ErrorResponseWriter
├─ AuthenticationManager Bean
└─ PasswordEncoder Bean

JwtAuthenticationFilter
├─ JwtProvider
├─ CustomUserDetailsService
└─ ErrorResponseWriter

CustomUserDetailsService
└─ UserRepository

PostController
└─ PostService
   ├─ PostRepository
   ├─ CommentRepository
   ├─ LikeRepository
   ├─ PostReportRepository
   ├─ ImageDataUrlValidator
   └─ ViewCountUpdater

프론트 Page
└─ feature API module
   └─ api.js request
      └─ fetch
```

이 그래프는 시작 지도이지 모든 호출을 다 기록한 최종 그래프가 아닙니다. 다음 AI는 기능별 문서에서 실제 호출 메서드와 반환값 위치를 추가해야 합니다.

---

## 11. 지금까지 사용자가 지적한 오류와 반드시 반영할 교훈

### 11.1 코드 일부 생략

문제: `SignupPage.jsx`에서 실제 코드에 있는 `errors` state를 설명에서 빠뜨린 적이 있습니다.

교훈: state·field·상수·함수 호출을 임의로 줄이지 않습니다. 생략하면 실제 코드와 설명의 흐름이 달라져 학습을 방해합니다.

### 11.2 주석 위치가 멀리 떨어짐

문제: 코드 앞의 번호가 코드 한참 아래의 설명을 가리키거나, `1` 코드 아래에 `2,3,4` 설명이 멀리 떨어진 적이 있습니다.

교훈: 설명 대상 코드 바로 위·아래에 둡니다. 관련 번호를 병합할 수는 있지만 멀리 보내지 않습니다.

### 11.3 두 주석본의 중복

문제: 전체 주석본과 목차 주석본을 따로 만들면서 실행 코드가 반복되고 순서가 어긋났습니다.

교훈: 현재는 하나의 코드 블록에 실제 코드와 line comment, block comment, 상세 설명을 함께 넣습니다.

### 11.4 결과만 적고 원리를 생략

문제: `response.addHeader(...)`가 왜 반환 body에 포함되지 않아도 응답에 들어가는지, `HttpHeaders.SET_COOKIE`가 어디서 온 값인지 등을 단순히 “Cookie를 추가한다”로만 설명한 적이 있습니다.

교훈: `HttpServletResponse`가 Controller 메서드에 전달되는 이유, header mutation, DispatcherServlet이 반환 객체를 Jackson으로 직렬화하는 시점까지 설명해야 합니다.

### 11.5 Spring Bean의 실행 시점 혼동

문제: `AuthenticationManager` Bean이 로그인 검증을 자동 실행하는 것처럼 설명한 적이 있습니다.

현재 정정:

- `@Bean authenticationManager(...)` 메서드는 애플리케이션 시작 시 객체를 준비한다.
- 실제 인증 검증은 `SessionService.createSession()` 내부의 `authenticationManager.authenticate(...)`가 로그인 요청 중 호출될 때 실행된다.
- `deleteSession()`과 `refreshSession()`은 현재 코드에서 `AuthenticationManager.authenticate()`를 호출하지 않고, Refresh Token hash와 `AuthSession`을 사용한다.

### 11.6 YAML placeholder 의미

문제: `${NAME:default}` 문법을 입력값이 없을 때 기본값을 쓰는 기능과, 연결 timeout 자체의 의미를 혼동한 적이 있습니다.

교훈: placeholder 해석과 실제 설정값의 동작 의미를 분리해 설명합니다. 예를 들어 `${REDIS_CONNECT_TIMEOUT:2s}`는 환경변수 값이 있으면 그 값을 사용하고 없으면 `2s`를 사용한다는 뜻입니다. `2s`가 timeout 설정이므로 결과적으로 연결 시도 제한 시간이 2초가 될 수 있지만, 두 문장은 같은 설명이 아닙니다.

### 11.7 Actuator health·보안·배포 경계

`management.endpoints.web.exposure.include=health`는 health endpoint를 HTTP endpoint로 노출하는 설정입니다. 로그인 없이 접근 가능한지는 `SecurityConfig`의 `requestMatchers(HttpMethod.GET, "/actuator/health").permitAll()`에서 별도로 결정합니다. YAML의 배포 환경변수 주입, Compose, Nginx, health 확인 과정은 배포 장에서 실제 연결을 추적해야 합니다.

### 11.8 Origin 없는 요청

Origin이 없는 요청을 허용한다고 해서 모든 CSRF 상황을 차단하는 것은 아닙니다. 반대로 Origin 없는 요청을 일괄 차단하면 curl, 서버 간 요청, 기존 MockMvc 테스트에 영향을 줄 수 있습니다. 현재 정책을 설명할 때는 실제 `SessionOriginInterceptor`, `InterceptorConfig`, 테스트 요청 header를 대조하고 “일부러 열어둔 것인지”와 “현재 도메인·HTTPS 조건”을 구분해야 합니다.

### 11.9 동시성·DB 제약

동시 중복 신고나 Refresh 동시 요청에 대해 DB unique 제약이 발생한다고 해서 항상 업무 예외 `Already_Reported`로 변환된다고 단정하면 안 됩니다. 실제 catch, exception translator, constraint 이름, 테스트 결과를 확인해야 합니다.

### 11.10 Front React 개념

다음 개념은 사용자가 실제 코드 위치에서 다시 설명받기를 원합니다.

- `Outlet`: 부모 route가 자식 route element를 렌더링할 자리로 제공하는 React Router 컴포넌트
- `lazy`: 동적 import의 Promise를 React가 렌더링할 수 있는 lazy component로 감싸는 함수
- `Suspense`: lazy component 등이 준비될 때 fallback을 표시하는 경계
- `key={location.key}`: 위치 key가 바뀔 때 React가 경계를 다른 인스턴스로 취급하게 하는 식별자
- `useEffect`: 컴포넌트 렌더링 외부의 작업을 실행하는 Hook; cleanup과 비동기 응답의 stale 상태 방지 흐름을 실제 코드와 함께 설명
- `AbortController`, `IntersectionObserver`: 게시글 목록 요청 취소·다음 페이지 감지에 사용되는지 실제 코드로 확인

사용자가 이미 질문한 개념이라도 그 개념이 다른 실제 코드에 다시 나오면 해당 코드 위치에서 흐름을 다시 설명합니다.

---

## 12. 아직 정독하지 않은 영역

현재 문서가 완료된 것은 전체 프로젝트가 아닙니다. 다음 영역은 아직 실제 원문·호출 관계·테스트를 모두 정독하지 않았습니다.

- 전체 Post·Comment Entity
- 전체 Post·Comment DTO
- 전체 Repository query와 JPQL
- `PostService.java`, `CommentService.java`, Controller 전체
- 프론트 `main.jsx` 이후 실행·라우팅 파일의 현재 원문
- 전체 `api.js`, `authApi.js`, `postApi.js`, `userApi.js`
- 게시글 목록·페이징·무한 스크롤 전체
- 게시글 상세·작성·수정·댓글·회원정보 화면
- Redis 조회수 전체 구현과 fallback
- Flyway V1~V4와 RDS 연결 설정
- H2·MySQL·Redis·Testcontainers·AOF·동시성 테스트
- backend/frontend Dockerfile, Compose, `.env.example`, Nginx, deploy script, GitHub Actions

참조되지 않는 파일이라고 표시하려면 import뿐 아니라 route, 문자열, annotation, reflection, workflow 참조를 먼저 검색해야 합니다.

---

## 13. 다음 AI가 이어서 할 권장 순서

### 전체 재배치 원칙

실행 시점과 학습 시점을 분리한다. Filter가 요청 중 먼저 실행된다는 이유로 Filter가
사용하는 wrapper·service보다 먼저 설명하지 않는다. 반대로 실제 사용처가 없는 파일을
단순히 폴더가 같다는 이유로 먼저 설명하지 않는다. 아래 순서는 현재 저장소의 import,
메서드 호출, route, 설정 key를 확인해 정한 학습 순서다.

### 1. 기반 실행

`settings.gradle` → `build.gradle` → `application.yaml` → `application-local.yaml` →
`application-prod.yaml` → `src/test/resources/application-test.yaml` →
`SpringdatajpaApplication.java` → 프론트 `package.json` → `vite.config.js` → `index.html` →
`src/main.jsx` → `src/App.jsx`.

Gradle wrapper 내부 구현은 버전 고정 역할만 확인하고 스킵한다. `data.sql`은 local의
`spring.sql.init.mode: always`와 연결되는지 확인한 뒤 Flyway 장에서 schema와 함께 다시 본다.

### 2. 프론트 라우팅 기반

`AppRoutes.jsx` → `ProtectedRoute.jsx` → `PublicOnlyRoute.jsx` → `PageBoundary.jsx` →
`PageLayout.jsx` → `AuthLayout.jsx` → `AppHeader.jsx`.

이 단계에서 `Outlet`, `lazy`, `Suspense`, `ErrorBoundary`, `useLocation`, route guard의
문법을 처음 등장한 코드 바로 아래에 설명한다.

### 3. 공통 응답·검증·예외

`ErrorResponseDto.java` → `ApiErrorCode.java` → `GlobalExceptionHandler.java` →
`ErrorResponseWriter.java` → 첫 예외 class → 나머지 `exception/*` →
`ImageDataUrlValidator.java`.

Controller가 던지는 예외와 Filter/Interceptor가 직접 response를 쓰는 경로를 먼저
분리해 둬야 이후 인증·게시글 오류 흐름을 혼동하지 않는다.

### 4. 회원가입

`User.java` → 회원가입 Request/Response DTO → `UserRepository.java` → `UserService.java` →
`UserController.java`.

이 단계에서 Entity, DTO 역직렬화·검증, Repository method-name query, Service 업무 순서,
Controller JSON 응답을 처음부터 끝까지 연결한다.

### 5. 인증 모델·Provider·세션 저장

인증의 흐름별 분류와 첫 학습 파일은
`00_실제코드_파일목록과_학습지도.md`의 `인증 흐름별 분류와 읽기 시작점`을 기준으로
확인한다. 기존 인증 문서의 설명은 다섯 흐름 문서로 재배치했고, 이전 원본은
`project-study/auth-legacy-backup`에 보관했다. 현재 학습 문서는 00번 지도에 따른다.

### 6. 로그인·보안 요청 흐름

`AuthenticationManager` Bean 생성과 `authenticate()` 호출 시점을 분리하고, Filter의
`filterChain.doFilter`와 Controller 진입을 실제 코드로 연결한다.

### 7. 게시글·댓글 Entity

`Post.java` → `PostImage.java` → `PostCounter.java` → `PostViewCount.java` → `Like.java` →
`PostReport.java` → `Comment.java`.

User에서 이미 본 mapping·soft delete 문법은 반복하지 않고, 각 Entity의 다른 상태 변경과
호출 Service를 확인한다.

### 8. 게시글·댓글 DTO·Repository

게시글 DTO 전체 → 댓글 DTO 전체 → `PostResponseFactory.java` → `PostRepository.java` →
`PostCounterRepository.java` → `CommentRepository.java` → `LikeRepository.java` →
`PostReportRepository.java` → `PostViewCountRepository.java`.

Page/Pageable, JPQL, bulk update, lock은 최초 코드에서 설명하고 동일한 query 구조는
사용 위치와 차이만 적는다.

### 9. 게시글·댓글 Service·Controller

`ViewCountUpdater.java` → `DatabaseViewCountUpdater.java` → `PostService.java` →
`CommentService.java` → `PostController.java` → `CommentController.java`.

목록·상세·작성·수정·삭제·좋아요·신고·댓글의 입력부터 response까지 각각 완성한다.

### 10. 프론트 공통 요청·인증 화면

`authStorage.js` → `api.js` → `authApi.js` → 공통 form 컴포넌트 → `LoginForm.jsx` →
`SignupForm.jsx` → `LoginPage.jsx` → `SignupPage.jsx`.

회원가입·로그인 endpoint와 401 refresh 호출의 실제 반환값을 백엔드 4~6단계와 연결한다.

### 11. 게시글 목록·페이징

`postApi.js` → `postNormalizer.js` → `PostsPage.jsx` → `PostBoard.jsx` → `PostList.jsx` →
`PostListItem.jsx` → 목록 표시 컴포넌트.

`useState`, `useEffect`, `useRef`, `useCallback`, `AbortController`,
`IntersectionObserver`는 실제 첫 등장 줄에서 설명하고, `Pageable`과 응답 page metadata의
왕복을 연결한다.

### 12. 상세·작성·수정·댓글·회원정보

`PostDetailPage.jsx` → 상세·댓글 컴포넌트 → `PostCreatePage.jsx` → `PostEditPage.jsx` →
`PostEditor.jsx` → 이미지 선택 컴포넌트 → `userApi.js` → 회원정보·비밀번호 페이지와 form.

공통 UI와 이미 설명한 form 문법은 스킵하고 callback, API endpoint, state 차이만 확인한다.

### 13. Redis 조회수

`ViewCountProperties.java` → `RedisViewCountStore.java` → `ViewCountPersistenceService.java` →
`RedisViewCountFlushScheduler.java` → `PostViewCountRepository.java` →
`DatabaseViewCountUpdater.java`.

Redis key·dirty set·분산 lock·snapshot·DB 반영·fallback을 9단계의 조회수 계약과 연결한다.

### 14. Flyway·RDS

`B3__current_schema.sql` → `V1__migrate_post_counters.sql` → `V2__add_user_auth_version.sql` →
`V3__expand_post_view_counts.sql` → `V4__remove_legacy_post_counter_view_count.sql` →
prod YAML → `FlywayBaselineMigrationTest.java` → `MySqlSchemaIntegrationTest.java`.

### 15. 테스트

Context test → Entity test → DTO factory test → auth/config test → Service Mockito test →
Controller 위임 test → H2 Repository integration test → MySQL schema/동시성 test →
Redis mock/integration/Testcontainers/AOF test 순서로 읽는다. 각 테스트의 profile,
container, DB, 검증하지 않는 범위를 `application-test.yaml`과 함께 기록한다.

### 16. 배포

백엔드 `Dockerfile` → backend `.env.example` → backend `compose.yaml` → backend Nginx →
`deploy-backend.sh` → backend workflow, 이후 프론트 `Dockerfile` → frontend `.env.example` →
frontend `compose.yaml` → frontend Nginx template → `deploy-frontend.sh` → frontend workflow.

Compose·Nginx·Shell·GitHub Actions 문법은 backend에서 자세히 설명하고 frontend는 실제
port/image/proxy/env 차이만 확인한다.

---

## 14. 파일별 문서 작성 전 체크리스트

다음 AI는 파일 하나 또는 관련 파일 묶음을 문서에 추가하기 전에 아래를 모두 확인합니다.

- [ ] 실제 절대 경로를 적었는가?
- [ ] 현재 원문을 다시 읽었는가?
- [ ] import와 annotation을 확인했는가?
- [ ] public 메서드와 흐름상 중요한 private 메서드의 호출 위치를 검색했는가?
- [ ] 호출자·호출 대상·매개변수 출처·반환값 도착 위치를 적었는가?
- [ ] DB·Redis·HTTP response·SecurityContext 변경 여부를 적었는가?
- [ ] 정상 흐름과 예외 흐름을 둘 다 적었는가?
- [ ] 다른 파일에서 이해에 필요한 연결 코드가 있으면 발췌했는가?
- [ ] 새 문법·annotation·라이브러리 API를 코드 바로 아래에 설명했는가?
- [ ] `package`와 `import`에 주석을 달지 않았는가?
- [ ] 코드 원문을 임의로 생략하지 않았는가?
- [ ] 생략했다면 발췌 이유와 확인 범위를 적었는가?
- [ ] 주석 번호가 실제 코드 가까이에 있는가?
- [ ] 결과뿐 아니라 작동 이유를 설명했는가?
- [ ] 문서의 문장이 실제 코드보다 강한 주장을 하지 않는가?
- [ ] 문서 추가 후 `git diff --check` 등 문서 형식 검사를 실행했는가?
- [ ] 진행률을 실제 확인 파일 수에 맞게 갱신했는가?

---

## 15. 파일 변경·검증 상태

이 인수인계 문서를 작성하기 전 확인한 상태:

- 백엔드 소스 저장소: 코드 변경 없음
- 프론트엔드 소스 저장소: 코드 변경 없음
- study12 문서 저장소: `00_실제코드_파일목록과_학습지도.md`, `99_작업인수인계_다음AI용.md`의 전체 학습 순서·진행률 갱신, 인증 관련 기존 문서를 5개 실행 흐름 문서로 재구성하고 기존 문서는 `project-study/auth-legacy-backup`으로 이동
- 이 문서 작성으로 `99_작업인수인계_다음AI용.md`가 추가됨
- 문서의 파일 목록과 경로는 현재 `rg --files` 결과를 기준으로 작성함
- 코드 테스트는 이 문서 작성 작업에서 실행하지 않음
- 문서 whitespace 검사: 새 보안 묶음 문서와 인수인계 문서에 trailing whitespace 없음

실행하지 않은 테스트를 완료했다고 보고하지 않습니다. 다음 AI가 코드 변경을 하게 되면 변경 파일, 테스트 명령, 실제 결과, 실행하지 못한 검증과 이유를 최종 보고에 분리해서 적어야 합니다.

---

## 16. 다음 AI에게 보내는 핵심 지시

이 프로젝트의 학습 목적은 “파일 이름을 나열하는 것”이 아니라 실제 코드의 데이터 이동과 실행 순서를 이해하는 것입니다.

따라서 다음 AI는:

1. 먼저 이 문서와 `00_실제코드_파일목록과_학습지도.md`를 읽는다.
2. 현재 문서의 진행률을 그대로 믿지 말고 다음 파일을 실제로 다시 확인한다.
3. 사용자가 “다음”이라고 하면 바로 파일을 만들기 전에 다음 관련 파일 묶음과 읽는 이유를 짧게 제시한다.
4. 사용자가 승인한 범위 안에서 한 번에 하나의 실행 흐름 또는 연관 파일 묶음만 문서화한다.
5. 실제 코드가 이전 문서와 다르면 이전 문장을 방어하지 않고 불일치를 먼저 보고한다.
6. 코드 아래 상세 설명에는 반드시 호출 관계, 매개변수 출처, 내부 순서, 반환값 사용처, 예외 이동, 구현 이유를 적는다.
7. 프론트·백엔드·설정·테스트·배포를 서로 독립된 파일 목록으로 설명하지 말고, 요청 하나가 양쪽을 어떻게 통과하는지 연결한다.
8. 설명이 부족하다는 사용자 피드백을 받으면 단순히 “더 자세히 하겠다”고만 하지 말고, 부족했던 원리·흐름·문법·예외·근거를 다음 문서에 실제로 추가한다.

가장 중요한 현재 지점은 다음 한 문장입니다.

> `@Bean`은 애플리케이션 시작 시 객체를 준비하는 선언이고, 그 객체의 업무 메서드는 실제 호출 코드가 실행될 때 동작한다. 설명에서는 객체 생성 시점과 업무 메서드 호출 시점을 절대 섞지 않는다.

---

## 17. 11장 문서 형식 오류와 최종 수정 기준

11장 문서를 작성하는 과정에서 다음 오류가 발생했습니다.

- 사용자는 파일별 분리를 요구했는데 메서드별 코드 블록으로 잘못 나누었습니다.
- 기존 설명의 순서만 바꾸면 되는 작업에 새 설명과 새 구조를 불필요하게 추가하려 했습니다.
- 도구 입력 오류로 한 번의 수정 시도가 실패했습니다.
- 이 문제는 사용자의 설정 문제가 아니라 문서 편집 범위를 잘못 해석한 작업 오류였습니다.

앞으로 11장과 모든 연관 파일 문서를 작성·수정할 때는 다음 형식만 사용합니다.

```text
[JwtAuthenticationFilter.java 전체 코드 한 블록]
[JwtAuthenticationFilter.java의 코드 일부]
[그 코드 일부에 대한 설명]
[다음 코드 일부]
[그 코드 일부에 대한 설명]

[CustomUserDetailsService.java 전체 코드 한 블록]
[CustomUserDetailsService.java의 코드 일부]
[그 코드 일부에 대한 설명]
[다음 코드 일부]
[그 코드 일부에 대한 설명]

[CustomUserDetails.java 전체 코드 한 블록]
[CustomUserDetails.java의 코드 일부]
[그 코드 일부에 대한 설명]
[다음 코드 일부]
[그 코드 일부에 대한 설명]
```

세부 규칙:

1. 메서드별로 파일의 전체 코드를 흩어놓지 않습니다. 전체 코드는 파일당 한 번만 보여줍니다.
2. 전체 코드 뒤에는 설명할 코드 일부를 그대로 다시 보여주고, 바로 아래에 그 부분의 설명을 둡니다.
3. 설명 앞의 코드 일부가 어느 설명을 가리키는지 번호로 표시하지 않습니다. 코드 자체를 설명의 기준으로 사용합니다.
4. 서로 다른 파일의 전체 코드를 하나의 블록에 섞지 않습니다. 한 파일의 전체 코드와 부분 코드·설명이 끝난 뒤 다음 파일로 이동합니다.
5. 이번 작업은 실제 백엔드·프론트엔드 코드를 수정하는 작업이 아니라 학습 문서의 순서와 배치를 수정하는 작업입니다.
6. 인증 문서를 다섯 흐름으로 재배치한 것은 문서 배치 변경이므로 파일 진행률을 증가시키지 않습니다. 현재 공식 파일 진행률은 41/213(약 19.2%)입니다.
7. Type variable = methodCall(argument) 형태의 대입문은 결과만 쓰지 않습니다. 왼쪽 타입·변수의 의미, 오른쪽 호출 객체, 매개변수의 출처, 메서드 내부의 조회·변환, 반환값이 왼쪽 변수에 저장된 뒤 어디에서 사용되는지까지 그 코드 일부 아래에 적습니다.
8. 7번은 특정 대입문 하나에만 적용하는 규칙이 아닙니다. 문서에 포함하는 모든 코드에 적용합니다. 모든 중요한 줄과 코드 구간에 대해 실행 주체, 실행 시점, 문법 구조, 값의 출처, 내부에서 호출되는 메서드, 생성·변경되는 상태, 반환값의 전달 위치, 예외 이동 경로, 왜 필요한지를 사용자 질문 전에 설명합니다.
9. 메서드 호출은 메서드가 선언된 파일·class·호출자·매개변수 출처·반환값 사용처를 적습니다. field와 상수는 선언 위치·초기화 방법·주입 주체·사용 위치를 적습니다. framework 객체는 누가 생성·전달·호출하는지 적습니다. 조건문·반복문·형변환·람다·체이닝도 현재 코드에서 실제로 어떤 값과 흐름을 만드는지 적습니다.
10. “호출한다”, “저장한다”, “응답한다”처럼 결과만 적는 주석으로 끝내지 않습니다. 그 결과가 생기는 원리와 다음 코드에서 사용되는 이유까지 해당 코드 일부 바로 아래에 둡니다. 사용자가 먼저 질문해야 설명을 추가하는 방식은 금지합니다.
11. Spring·Servlet·React 등 외부 framework가 제공하는 class·interface·Hook·객체는 framework가 제공한다는 사실과 현재 코드에서의 역할을 해당 코드 일부 바로 위 주석과 아래 설명에 적습니다. import 줄에는 주석을 달지 않습니다.
12. Filter·middleware·router처럼 다음 처리 단계로 넘기는 코드는 단순히 “다음으로 전달한다”고 쓰지 않습니다. 누가 다음 객체를 호출하는지, 그 호출이 Controller까지 어떻게 이어지는지, 현재 method의 return이 downstream 호출을 취소하지 않고 현재 method만 종료하는 이유를 실행 순서로 설명합니다. 인증하지 않는 것과 공개 endpoint로 허용하는 것도 구분합니다.
13. `throw`, `throws`, `orElseThrow`처럼 실행과 선언이 비슷해 보이는 문법은 구분해서 설명합니다. 실제로 예외를 발생시키는 코드, 예외 가능성을 메서드 선언에 적는 문법, 호출자에게 전파되는 경로를 각각 코드 일부 바로 아래에 적습니다.
