# Codex 작업 인수인계

작성일: 2026-07-30  
목적: 다른 컴퓨터에서 Codex와 새 대화를 시작할 때 이 파일을 첨부하여 현재 작업을 그대로 이어가기 위함

## 1. 프로젝트 위치와 역할

- `study11`
  - 부하 테스트 및 개선 전 기준 버전
  - 코드를 더 수정하지 않고 비교 기준으로 보존한다.
- `study12`
  - 동시성 제어 개선을 구현하고 부하 테스트하는 버전
- `KTB4_Miles_Week12_Back`
  - 실제 배포되는 백엔드 저장소
  - `study12`에서 구현과 검증을 끝낸 뒤 최종 코드를 반영한다.
- `KTB4_Miles_Week12_Front`
  - 실제 배포되는 프론트엔드 저장소
  - API 변경으로 프론트 수정이 필요할 때만 반영한다.

주요 절대 경로:

- `/Users/miles/Documents/GitHub/kakao_tech_bootcamp_study/study11`
- `/Users/miles/Documents/GitHub/kakao_tech_bootcamp_study/study12`
- `/Users/miles/Documents/GitHub/KTB4_Miles_Week12_Back`
- `/Users/miles/Documents/GitHub/KTB4_Miles_Week12_Front`

다른 컴퓨터에서는 저장 위치가 달라질 수 있으므로 저장소 이름과 폴더 역할을 기준으로 찾는다.

## 2. 작업 목적

트래픽이 인기 게시글 하나에 몰릴 때 발생하는 MySQL Hot Row 문제를 재현하고, 다음 구조로 개선한다.

1. 조회 요청마다 MySQL 행을 갱신하지 않는다.
2. Redis의 원자적 증가 연산으로 조회수를 누적한다.
3. 조회수용 MySQL 테이블을 다른 카운터와 분리한다.
4. 스케줄러가 Redis 조회수를 주기적으로 MySQL에 반영한다.
5. 백엔드가 여러 대일 때는 분산 락으로 스케줄러 중 하나만 반영 작업을 수행하게 한다.
6. 같은 부하 조건으로 `study11`과 `study12` 결과를 비교한다.

캐시 스탬피드 실험은 범위에서 제외한다.

## 3. 반드시 유지할 설계 결정

### 요청 처리

- 게시글 조회 한 건마다 Redis에서 조회수를 원자적으로 1 증가시킨다.
- 조회 요청 경로에는 분산 락을 걸지 않는다.
- Redis `INCR` 자체가 한 키에 대한 원자성을 보장한다.
- 분산 락은 속도를 높이기 위한 장치가 아니다.

### 주기적 MySQL 반영

- 여러 백엔드 인스턴스의 스케줄러가 동시에 같은 반영 작업을 하지 않도록 Redisson 분산 락을 사용한다.
- 락을 획득한 스케줄러 하나만 공유 Redis의 값을 공유 MySQL에 반영한다.
- 다른 서버가 요청 처리를 멈추는 것은 아니다. 모든 서버는 계속 Redis 조회수를 증가시킨다.
- MySQL에는 증가량을 단순히 더하는 방식보다 Redis의 절대 스냅샷을 반영한다.
- 재실행되어도 값이 감소하거나 중복 증가하지 않도록 `GREATEST(현재값, 스냅샷)` 형태의 멱등성을 유지한다.

### 데이터 구조

- `post_counters`
  - 좋아요 수, 댓글 수, 신고 수에 대한 기존 MySQL 트랜잭션 처리는 우선 유지한다.
- `post_view_counts`
  - 조회수만 별도 행으로 분리한다.
- 기존 `post_counters.view_count` 열은 첫 배포에서 삭제하지 않는다.
  - 구버전 Blue와 신버전 Green이 같은 DB를 사용해도 호환되어야 한다.
  - 롤백 가능성도 유지한다.
- 모든 카운터를 한꺼번에 분리하지 않는다.
  - 개선 후 측정 결과로 추가 분리가 필요한지 판단한다.

### Redis 장애 시 방향

- Redis가 실패했을 때 요청마다 MySQL 조회수를 직접 갱신하는 우회는 Hot Row를 다시 만들 수 있다.
- 현재 방향은 게시글 응답은 마지막 DB 반영값으로 제공하고, 조회수 증가는 건너뛰며 장애를 기록하는 방식이다.
- 실제 구현 단계에서 이 정책을 테스트로 명확히 고정한다.

## 4. 기존 코드의 동시성 제어 해석

- 게시글 수정 등 충돌 가능 작업에는 기존 MySQL 락을 유지한다.
- 좋아요 처리에는 역할이 서로 다른 장치가 함께 있다.
  - 유니크 제약: 한 사용자의 중복 좋아요 방지
  - 원자적 증가: 좋아요 카운터의 유실 갱신 방지
  - 비관적 락: 좋아요 생성·삭제와 카운터 변경을 하나의 상태 전이로 직렬화
- 장치가 여러 개 있다고 무조건 중복은 아니다. 각각 보호하는 불변 조건이 다르다.
- 이번 측정으로 확인한 병목은 특히 조회수와 댓글 카운터가 같은 `post_counters` 행을 경쟁하는 구조다.

## 5. 완료된 `study11` 기준 테스트

사용 도구:

- k6: 여러 가상 사용자가 HTTP 요청을 보내도록 하는 부하 생성기
- Docker Compose: MySQL, 백엔드, k6 실행 환경 고정
- MySQL 관측 스크립트: InnoDB 행 락 대기 횟수와 시간을 같은 구간에 수집

테스트 원칙:

- Codex가 의미 있는 부하 테스트를 대신 실행하지 않는다.
- 사용자가 명령어를 직접 실행하며, Codex는 실행 전에 명령의 의미와 관측 지표를 설명한다.
- 자동화된 단위·통합 테스트와 개발용 로컬 검증은 Codex가 실행할 수 있다.

### 인기 게시글 용량 테스트 결론

- 완료 요청: 29,979건
- 버려진 반복: 9,271건
- HTTP 응답 시간 p95: 2.36초
- MySQL 행 락 대기 증가: 21,747회
- 누적 락 대기 시간 증가: 392,909ms

결론:

- 요청마다 같은 MySQL 조회수 행을 갱신하는 방식은 인기 게시글 트래픽에서 병목이 된다.
- 따라서 조회 요청 경로를 Redis 원자적 증가로 변경할 근거가 있다.

### 댓글 단독과 조회+댓글 혼합 테스트 결론

댓글 단독 3회 중앙값:

- 생성 p95: 14.12ms
- 생성 p99: 17.53ms
- 삭제 p95: 8.29ms
- 삭제 p99: 13.07ms
- 전체 생명주기 p95: 22ms
- 전체 생명주기 p99: 29ms
- MySQL 락 대기: 0회

같은 댓글 부하와 초당 조회 200건을 섞은 3회 중앙값:

- 생성 p95: 29.04ms
- 생성 p99: 77.47ms
- 삭제 p95: 16.34ms
- 삭제 p99: 66.66ms
- 전체 생명주기 p95: 52ms
- 전체 생명주기 p99: 181.21ms
- 락 대기 횟수: 1,400회 / 2,105회 / 1,658회
- 회차별 락 대기 횟수 산술 평균: 1,721회
- 누적 락 대기 시간: 15,299ms / 37,730ms / 24,607ms
- 회차별 누적 락 대기 시간 산술 평균: 25,878.67ms
- 총 락 대기 5,163회, 총 대기 77,636ms
- 한 번의 락 대기당 평균: 약 15.04ms

결론:

- 댓글 부하 자체는 문제가 없지만, 조회수 갱신과 같은 `post_counters` 행을 사용할 때 댓글 지연까지 증가한다.
- 데이터의 논리적 관계와 무관하게 물리적으로 같은 행을 갱신해서 생기는 거짓 경합이다.
- 조회수를 `post_view_counts`로 분리할 근거가 된다.
- 유효하게 시작된 요청은 성공했고 댓글 카운터와 실제 행 수의 무결성도 유지되었다.

## 6. 보고서 증거 자료

다음 파일은 `study12/tools/loadtest/results` 아래에 만들어졌고 보고서에 첨부를 완료했다.

- `portfolio-01__study11-test-conditions-and-integrity.md`
- `portfolio-02__study11-comment-latency-comparison.md`
- `portfolio-03__study11-mysql-lock-average-comparison.md`

`deployment-01__before-expand__code-evidence.md`는 사용자의 요청으로 삭제했다. 다시 만들지 않는다.

최종 테스트가 모두 끝난 뒤 해야 할 정리:

- 토큰 만료, 단순 smoke, observer-check 등 무의미한 결과 파일 삭제
- 유효한 결과만 유지
- `study11`과 `study12`, 테스트 종류, 1차·2차·3차를 이름만 보고 구분할 수 있도록 파일명 정리
- 이 정리는 최종 테스트 전에는 하지 않는다.

## 7. Redis 학습 및 환경 준비 완료 내용

학습 완료:

- Redis `SET` / `GET`
- 동시에 100번 `INCR`한 결과가 정확히 100이 되는 원자성 확인
- `SET key owner NX EX 300`으로 분산 락 획득 경쟁 확인
- 소유자 토큰을 검사하는 Lua 스크립트로 안전한 락 해제 확인

임시 학습 컨테이너 `bamboo-redis-practice`는 제거했다.

`study12` 부하 테스트 Compose 환경:

- Redis 이미지: `redis:7.4-alpine`
- 포트: `127.0.0.1:16380 -> 6379`
- AOF 사용
- `appendfsync everysec`
- Docker 볼륨 `redis-data`
- PING 헬스 체크

사용자가 컨테이너 재생성 뒤에도 값이 유지되는 것을 확인했다.

## 8. `study12`에 이미 구현된 코드

추가:

- `PostViewCount` 엔티티
- `PostViewCountRepository`
- Flyway `V3__expand_post_view_counts.sql`
- 엔티티 테스트
- 저장소 통합 테스트

변경:

- `Post`와 `PostViewCount`를 `@MapsId` 기반 일대일 관계로 연결
- 새 게시글 생성 시 조회수 엔티티도 0으로 생성
- H2용 `data.sql`에서 기존 조회수를 새 테이블로 복사

V3 마이그레이션:

- `post_view_counts`를 추가한다.
- 기존 `post_counters.view_count`를 새 테이블로 백필한다.
- 이미 값이 있다면 더 큰 값을 보존한다.
- 기존 조회수 열은 삭제하지 않는다.

검증 완료:

- 전체 Gradle 테스트 성공
- `git diff --check` 성공
- MySQL 8.4에서 새 테이블의 열 타입, NULL 여부, 기본값 확인
- 기존 2행과 새 테이블 2행의 조회수 불일치 0건 확인

현재 단계의 중요한 한계:

- 아직 실제 게시글 조회 요청은 기존 MySQL 조회수 갱신 경로를 사용한다.
- DTO도 기존 조회수 값을 사용한다.
- 따라서 지금은 스키마 확장만 완료한 상태이며 성능 개선 전이다.
- 이 중간 상태로 부하 테스트하지 않는다.

관련 변경은 모노레포 커밋 `1d40a9b0` (`view count`)에 푸시된 상태로 확인했다.

## 9. 아직 구현하지 않은 다음 작업

가장 먼저 할 일:

1. `study12` 백엔드에 Spring Data Redis와 Redisson 의존성 추가
2. Redis 연결 설정과 조회수 관련 설정 객체 추가
3. Redis 조회수 키와 dirty set을 원자적으로 갱신하는 서비스 구현
4. 동시 초기화에서도 기존 DB 조회수보다 작아지지 않도록 처리
5. 조회 API가 Redis의 최신 조회수를 반환하도록 변경
6. Redis 장애 시 정한 열화 정책 구현
7. Redisson 분산 락을 사용하는 주기적 MySQL 반영 스케줄러 구현
8. 중복 실행, 반영 중 새 증가, 락 획득 실패, Redis 장애 테스트
9. `study12` 전체 자동 테스트
10. `study12`용 k6 결과 조회가 새 조회수 구조를 읽도록 부하 테스트 도구 보완
11. 사용자가 `study11`과 동일한 조건으로 의미 있는 부하 테스트 실행
12. 결과 비교 후 필요할 때만 추가 개선
13. 최종 호환 버전을 실제 `KTB4_Miles_Week12_Back` 기능 브랜치에 반영
14. API가 그대로 숫자 `viewCount`를 반환한다면 프론트는 수정하지 않음
15. PR 검증 후 `main` 병합으로 기존 Blue/Green 자동 배포 수행
16. 모든 테스트 완료 뒤 결과 파일 정리 및 이름 변경

## 10. 배포 원칙

깔끔한 기본 절차를 유지한다.

1. `study12`에서 기능 전체 구현
2. 자동 테스트 완료
3. 사용자가 의미 있는 개선 후 부하 테스트 완료
4. 완성된 하위 호환 릴리스를 Week12 Back 기능 브랜치에 한 번에 동기화
5. PR과 CI 확인
6. `main` 병합
7. 기존 Blue/Green 배포 스크립트가 비활성 색상을 먼저 띄우고 헬스 체크 후 전환

V3가 추가형 마이그레이션이고 기존 열을 유지하므로 구버전 Blue와 신버전 Green의 DB 호환성을 확보한다.

별도의 3단계 애플리케이션 배포를 필수로 만들지 않는다. 기존 열 삭제는 나중에 정말 필요할 때 별도의 정리 릴리스로 한다.

`study12` 모노레포에 푸시하는 것은 실제 서비스 배포가 아니다. 실제 배포는 `KTB4_Miles_Week12_Back`의 `main`과 기존 CI/CD 조건에 의해 수행된다.

## 11. Codex가 반드시 지킬 협업 방식

- 사용자는 Redis와 부하 테스트를 처음 학습 중이다.
- 모든 코드 수정은 수정 전에 목적을 말하고, 수정 후 파일별로 무엇이 왜 바뀌었는지 설명한다.
- 처음 등장하는 용어와 문법은 생략하지 않고 설명한다.
- 사용자 질문 하나를 전체 계획 변경 요구로 과대해석하지 않는다.
- 더 좋은 방법을 근거로 선택하고, 이미 정한 안정적인 방법을 임의로 계속 바꾸지 않는다.
- 사용자에게 명령을 줄 때 항상 절대 경로를 사용한 `cd` 명령부터 제공한다.
- 명령마다 무엇을 실행하고 어떤 결과를 확인하는지 바로 설명한다.
- 의미 없는 로컬 확인이나 캡처를 사용자에게 반복시키지 않는다.
- 포트폴리오 가치가 있는 증거가 생길 때만 무엇을 캡처하고 어떤 원본 파일을 첨부할지 정확히 안내한다.
- Codex 화면을 증거로 캡처하게 하지 않는다.
- `.DS_Store` 변경은 사용자의 변경이므로 건드리거나 커밋하지 않는다.
- `study11`은 기준 버전이므로 수정하지 않는다.
- 사용자가 실행해야 하는 의미 있는 k6 부하 테스트를 Codex가 대신 실행하지 않는다.
- 각 단계가 끝날 때 항상 남은 목차를 보여준다.

## 12. 새 대화에서 사용할 시작 요청

새 컴퓨터에서 이 파일을 Codex에 첨부하고 다음과 같이 요청한다.

> 이 인수인계 파일을 기준으로 작업을 이어가 줘. 먼저 실제 저장소 상태가 문서와 일치하는지 읽기 전용으로 확인하고, 차이가 있으면 알려줘. 임의로 전체 계획을 바꾸지 말고 다음 단계인 study12 Redis 의존성과 연결 설정부터 진행해 줘. 모든 코드 수정은 내가 이해할 수 있게 파일별로 설명하고, 의미 있는 부하 테스트는 네가 실행하지 말고 내가 실행할 수 있게 알려줘. 답변 끝에는 남은 목차를 보여줘.

